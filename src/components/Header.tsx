import React from 'react';

export const Header = () => {
    return (
        <div style={{textAlign: 'center'}}>
            <span className="app-title">
                Expense tracker
            </span>
        </div>
    )
}
